kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: worker
    extraMounts:
      - hostPath: $HL_MOUNT
        containerPath: /mnt/hyperledger
  - role: control-plane
    extraMounts:
      - hostPath: $HL_MOUNT
        containerPath: /mnt/hyperledger
